using UnityEngine;

public class LancerController : EnemyController
{
    //// --- Methods ---
    //protected override void Start()
    //{
    //    base.Start();

    //    // Check if we have an attack action and set the chase range radius to the attack range area radius.
    //    AttackAction attackAction = GetComponent<AttackAction>();
    //    if (attackAction != null && move != null) {
    //        move.chaseRangeRadius = attackAction.suggestedDistance;
    //    }
    //}
}
